Place your app logo files here so packaged builds use your brand icon.

Required files:
- `icon.ico` for Windows installer/exe
- `icon.icns` for macOS dmg/app
- `icon.png` for Electron window icon (Windows/Linux runtime)

Recommended source size: at least 1024x1024 square.
